"""
The MongoDB package contains the tools to interact with the MongoDB database. The MongoDB database is used to store the
data that is extracted from the Spotify API. The data is stored in collections such as 'liked_songs', 'albums', 'artists',
'playlists', 'recommendations'.

"""